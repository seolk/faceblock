<%= form_with(model: @post, class: 'ui form') do |f| %>
    <%= f.label :content %><br />
    <%= f.text_area :content, required:true %>
    <br />
    
    <%= f.submit nil, class: 'ui button primary' %>
    
<% end %>