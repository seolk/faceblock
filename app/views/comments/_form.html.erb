<%= form_with(model: @comment, class: 'ui form') do |f| %>
    <%= f.label :body %><br />
    <%= f.text_area :body, required:true %>
    <br />
    
    <%= f.submit nil, class: 'ui red button' %>
    
<% end %>